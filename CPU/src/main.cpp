#include "CPU.h"

int main()
{
    printf("ITS ALIVE\n");
    // Stack stack = {}; 
    // StackCtor(&stack);
    // StackPush(&stack, 5);
    // StackPrint(&stack);
    // StackDtor(&stack);
    return 0;
}